for(int i = k; i >= 0; i--) {
    //     for(int j = 0; j < n; j++) {
    //         // Don't use the current coin, move to the next coin
    //         dp[i][j] = dp[i][j + 1];
    //         // Use the current coin, if i - coins[j] >= 0
    //         if(i - coins[j] >= 0) {
    //             dp[i][j] += dp[i + coins[j]][j];
    //         }
    //     }
    // }